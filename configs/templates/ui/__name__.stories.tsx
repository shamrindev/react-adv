import type { ComponentMeta } from '@storybook/react';
import { __name__ } from './__name__';

export default {
  title: 'shared/__name__',
  component: __name__,
} as ComponentMeta<typeof __name__>;

export const Primary = {
  args: {},
};
